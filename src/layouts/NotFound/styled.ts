import styled from "styled-components";

export const _NotFound = styled('div')`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    img {
        width: 50%;
    }
`